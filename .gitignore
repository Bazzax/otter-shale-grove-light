node_modules/
# Never commit Nitro/Vercel Build Output API artifacts. If
# `.vercel/output` is in git, Vercel skips `npm run build` and
# deploys the stale tree ("Using prebuilt build artifacts").
.vercel/output/
.project_id
.github_repo
.env
.env.*
